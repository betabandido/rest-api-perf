apiVersion: apps/v1
kind: Deployment
metadata:
  name: rocket-rest-api
spec:
  selector:
    matchLabels:
      app: rocket-rest-api
  replicas: 1
  template:
    metadata:
      labels:
        app: rocket-rest-api
    spec:
      containers:
      - name: rocket-rest-api
        image: gcr.io/product-catalog-237219/rocket-rest-api
        ports:
        - containerPort: 8000
        env:
        - name: REST_API_PERF_REPO
          value: in-memory
---
apiVersion: v1
kind: Service
metadata:
  name: rocket-rest-api
spec:
  selector:
    app: rocket-rest-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer